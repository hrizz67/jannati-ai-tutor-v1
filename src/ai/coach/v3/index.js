import { buildExplanation } from './explanationEngine.js';
import { buildHint } from './hintEngine.js';
import { buildPraise } from './praiseEngine.js';
import { buildLearningTips } from './learningTips.js';
import { getSubjectStrategy, getSubjectStrategyFields } from './subjectStrategies.js';
import { buildCoachResponse, getCoachPreview } from './coachController.js';

export {
  buildExplanation,
  buildHint,
  buildPraise,
  buildLearningTips,
  getSubjectStrategy,
  getSubjectStrategyFields,
  buildCoachResponse,
  getCoachPreview
};

export default {
  buildExplanation,
  buildHint,
  buildPraise,
  buildLearningTips,
  getSubjectStrategy,
  getSubjectStrategyFields,
  buildCoachResponse,
  getCoachPreview
};
