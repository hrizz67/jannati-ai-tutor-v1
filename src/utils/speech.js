export function speakText(text,lang='ms-MY'){if(!('speechSynthesis'in window)){alert('Browser ini tidak menyokong bacaan suara.');return;}window.speechSynthesis.cancel();const u=new SpeechSynthesisUtterance(text);u.lang=lang;u.rate=.88;window.speechSynthesis.speak(u);}export function beep(type='good'){try{const c=new(window.AudioContext||window.webkitAudioContext)();const o=c.createOscillator();const g=c.createGain();o.connect(g);g.connect(c.destination);o.frequency.value=type==='good'?720:type==='mid'?440:220;g.gain.value=.08;o.start();setTimeout(()=>{o.stop();c.close()},type==='good'?180:260)}catch(e){}}